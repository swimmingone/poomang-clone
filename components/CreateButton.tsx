import React from 'react';

const CreateButton = () => {
	return (
		<div className={'flex justify-center'}>
			<button className="btn btn-primary btn-circle text-2xl" type="button">
				+
			</button>
		</div>
	);
};

export default CreateButton;
